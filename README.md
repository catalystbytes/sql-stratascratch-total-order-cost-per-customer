# sql-stratascratch-total-order-cost-per-customer
SQL case study analyzing total customer order cost using joins and aggregation (GROUP BY). Demonstrates business metric calculation and clean, portable SQL design.
